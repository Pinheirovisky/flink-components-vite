import { ReferenceValueProps } from "@protocols/referenceValue";

const mockReferenceValue: ReferenceValueProps = {
  ferrovia: "ALLP",
  vlReferencia: {
    id: "02",
    valor: "",
  },
};

export { mockReferenceValue };
